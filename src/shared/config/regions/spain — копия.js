export const SpainConfig = {
  region: 'es',
  defaultLang: 'es',
  supportedLangs: ['es', 'en'],
  features: {
    showHomeHero: true,
    showHomeAbout: true,
    // Add feature flags here as needed
  }
};
